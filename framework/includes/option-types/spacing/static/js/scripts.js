/**
 * fw-option-type-spacing
 *
 * Currently a no-op placeholder. The widget is pure CSS + native <select>
 * sub-controls — no JS behaviour is needed for v1.
 *
 * future: link-sides toggle (Figma-style chain icon between per-side selects
 * that locks T/R/B/L together so changing one updates all four).
 */
(function ($) {
	'use strict';

	$(function () {
		// Intentionally empty.
	});
})(jQuery);
